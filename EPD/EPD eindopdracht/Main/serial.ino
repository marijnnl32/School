unsigned long previousMillis = 0;


void serialSetup() {
  Serial.begin(9600);
}


bool setTimer(int a{
  int interval = a if (millis() >= previousMillis + interval) {

    return (TRUE)
  }

  else {
    return (FALSE)'
 '
  }
}